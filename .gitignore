__pycache__/
*.pyc
.venv/
dist/
build/
*.egg-info/
.dev.vars
.env
# User tokens must NEVER be committed. The embedded package client
# (src/health_data_connect/data/google_client.json) is a public Desktop client
# and IS committed deliberately — do not add it here.
google_tokens.json
.pypi.token
.pypi_token
